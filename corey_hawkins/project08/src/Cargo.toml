[package]

name = "project 8"

version = "0.1.0"

edition = "2021"



[dependencies]

clap = { version = "4.4", features = ["derive"] } # For command-line argument parsing

csv = "1.3"                                     # For reading and writing CSV files

anyhow = "1.0"
